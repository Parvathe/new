apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
  namespace: ${K8S_NAMESPACE}
spec:
  ports:
  - name: svc-port
    port: 8085
    targetPort: 8085
    protocol: TCP
  selector:
    app: ${APP_NAME}
  type: ClusterIP
